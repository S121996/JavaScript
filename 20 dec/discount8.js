if (!isNaN(amount) && amount >= 0) {
    let discount;
var amount=300;
if(amount>=1000)
{
console.log(discount=0.15);
}
else{
    discount=0.05;
}
 var calculatediscount =amount*discount;
 var discountAmount=amount-calculatediscount;
 console.log('original amount');
}